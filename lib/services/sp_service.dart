import 'package:shared_preferences/shared_preferences.dart';

class SP {
  // static saveLoginSatate(bool isLogged) async {
  //   SharedPreferences preferences = await SharedPreferences.getInstance();
  //   preferences.setBool(loggedState);
  // }
}
